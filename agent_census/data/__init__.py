"""Versioned data lists (vuln paths, bot tokens) loaded at runtime.

Kept as plain text so the heuristics they drive can be inspected and evolved
without touching code. Loaded via :mod:`agent_census.dataload`.
"""
